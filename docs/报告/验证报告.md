# API 验证报告

> 由 `scripts/Verify-Api.ps1` 自动生成，用于实测校验 `docs/设计/API现状.md` §4 的各项判断。
>
> 执行时间：2026-09-02 15:56:20　｜　licence：`LICENCE-66D8-9F96-0C7F0FBCD073`

## T0 连通性与 licence 有效性

### 基础连通性

**✅ 与文档一致**

`hslt/list` 返回 **5203** 条，耗时 525 ms。


## T0.5 数据源真实性探针【前置门禁】

### 数据源真实性

**⚠️ 与文档不符**

| 请求代码 | 返回价格 `p` | 返回总市值 `sz` | 更新时间 `t` |
|---|---|---|---|
| `000001` | 11.91 | 231124485738 | 2026-09-02 15:05:39 |
| `600038` | 11.91 | 231124485738 | 2026-09-02 15:05:39 |
| `002230` | 11.91 | 231124485738 | 2026-09-02 15:05:39 |
> **三个完全不同的股票代码返回了完全相同的数据。**
>
> 结论：当前 licence **忽略股票代码参数，只返回一份固定样本数据**（实测为平安银行 000001）。
>
> **因此所有「按代码查询」的测试项均无法验证，下方会明确标记为 ⛔，而不会给出结论。**
> 要得到有效结论，请用正式 licence 重跑：
> `.\scripts\Verify-Api.ps1 -Licence "你的licence"`

> ⚠️ 当前使用的是官方文档中的**公开演示 licence**，本就只提供样本数据，非正式授权。


## T1 (§4.8) hslt/list 覆盖范围与 dm 字段真实格式

首条原始记录：

```json
{"dm":"000001.SZ","mc":"平安银行","jys":"SZ"}
```

### dm 字段格式

**⚠️ 与文档不符**

文档写 `dm` 形如 `000001`、`jys` 为小写 `sz`；实测 `dm` = `000001.SZ`（**已带市场后缀**），`jys` = `SZ`（**大写**）。

> **影响（与 §4.6 原结论相反）：** 调用 `hsstock/*` 时**无需自行拼接后缀**；反而调用 `hscp/*` / `hsrl/*` 时需要**主动去掉**后缀。

### hslt/list 覆盖范围

**ℹ️ 实测结果**

| 板块 | 数量 |
|---|---|
| 000/001 深主板 | 529 |
| 002/003 中小板 | 963 |
| 300/301 创业板 | 1397 |
| 600/601/603/605 沪主板 | 1704 |
| 688/689 科创板 | 609 |
| 4xx/8xx 北交所 | 0 |
| **合计** | **5203** |

- **包含科创板**（609 只），无需另调 `kc/list/all`。
- **不含北交所**，需另调 `bj/list/all`。


## T2 (§4.6) 代码参数格式：带后缀 vs 纯 6 位

### 代码参数格式

**⛔ 无法验证**

当前 licence 忽略股票代码，两种写法都会返回同一份样本数据，**无法据此判断格式要求**。需用正式 licence 重测。


## T3+T4 (§4.5/§4.7) 历史回溯深度与单次条数上限【最关键】

### 历史回溯深度（决定「17年后底」是否可行）

**⛔ 无法验证**

不带 `st`/`et` 请求 `hsstock/history/600038.SH/d/n`：

| 项目 | 实测值 |
|---|---|
| 返回条数 | **50** |
| 最早交易日 | **2025-09-15** |
| 最新交易日 | 2025-12-01 |
| 回溯年限 | 约 1 年 |
| 耗时 | 449 ms |
> ⛔ **该结果无效。** 当前 licence 返回的是固定样本（2025-09-15 ~ 2025-12-01，恒 50 条，且与请求代码无关）。
>
> 样本数据的条数上限与真实回溯深度**没有任何关系**，不能据此判定「17年后底」不可行。
>
> **这仍是整套方案的头号未决问题，必须用正式 licence 重跑本项。**

### 显式传 st 是否能取到更早数据

**⛔ 无法验证**

| 请求方式 | 条数 | 最早日期 |
|---|---|---|
| 不传 `st`/`et` | 50 | 2025-09-15 |
| 显式 `st=19970101` | 50 | 2025-09-15 |

> ⛔ 样本数据下两者必然相同，无参考意义。需用正式 licence 重测。


## T5 (§4.9) licence 是否需要「证书」前缀

### hsstock/real/time 的 licence 写法

**✅ 与文档一致**

| 写法 | 结果 |
|---|---|
| `/{licence}` | ✅ 可用 |
| `/证书{licence}` | ❌ 失败 |

> 文档地址模板中的 `/证书您的licence` **确为笔误**，实际只需传 licence 本身。


## T6 (§4.1) ZygdSdgd 对象的真实结构

### ZygdSdgd 真实结构（补齐文档缺失）

**ℹ️ 实测结果**

文档中 `ZygdSdgd` 无任何定义。实测请求 `hscp/sdgd/000001`，内层数组含 10 个元素，第 1 位股东字段如下：

| 字段 | 示例值 |
|---|---|
| `Pm` | 1 |
| `Gdmc` | 中国平安保险(集团)股份有限公司-集团本级-自有资金 |
| `Cgsl` | 9618540236 |
| `Cgbl` | 49.56 |
| `Gbxz` | 流通A股 |

> 外层记录字段：`jzrq`（截止日期）、`ggrq`（公告日期）、`gdsm`（股东说明）、`gdzs`（股东总数）、`pjcg`（平均持股）、`sdgd`（本数组）。
>
> **文档缺失部分至此已补齐**：`Pm` 排名、`Gdmc` 股东名称、`Cgsl` 持股数量、`Cgbl` 持股比例(%)、`Gbxz` 股本性质。注意字段名**首字母大写**，与其他接口的全小写风格不一致。


## T7 (§3.1) 补贴收入 btsr 与其他收益 qtsy

### 利润表 btsr / qtsy 实测（000001.SZ，2024-2025）

**ℹ️ 实测结果**

| 截止日期 | `btsr` 补贴收入 | `qtsy` 其他收益 | `yffy` 研发费用 | `yyzsr` 营业总收入 |
|---|---|---|---|---|
| 2025-09-30 | _(空)_ | 56000000 | _(空)_ | 100668000000 |
| 2025-06-30 | _(空)_ | 50000000 | _(空)_ | 69385000000 |
| 2025-03-31 | _(空)_ | 43000000 | _(空)_ | 33709000000 |
> ✅ **证实 §3.1 的判断**：`btsr` 全部为空（3/3 期），而 `qtsy` 有实值（3 期）。
> 政府补助确已改列报于「其他收益」，**取数应以 `qtsy` 为准，`btsr` 不可用**。


## T8 (§3.2) X 日价：指定日期取收盘价

### X 日价取数方案

**⚠️ 与文档不符**

指定单日却返回 **50** 条（首条 2025-09-15），说明 **`st`/`et` 参数未被接口正确处理**（当前为样本数据，需正式 licence 复测）。取数时必须在本地按日期二次过滤。


## T9 (§4.3) 科创板 / 北交所能否走 hsstock 历史接口

### 跨市场支持

**⛔ 无法验证**

当前 licence 忽略股票代码，任何代码都会返回同一份样本，**无法判断跨市场支持情况**。需用正式 licence 重测。


## T10 (§4.2) 限频实测【已跳过】

加 `-TestRateLimit` 开关可执行连续 40 次请求的压测，默认跳过以免消耗配额。


---

## 需要关注的结论

- 数据源真实性 — ⚠️ 与文档不符
- dm 字段格式 — ⚠️ 与文档不符
- 代码参数格式 — ⛔ 无法验证
- 历史回溯深度（决定「17年后底」是否可行） — ⛔ 无法验证
- 显式传 st 是否能取到更早数据 — ⛔ 无法验证
- X 日价取数方案 — ⚠️ 与文档不符
- 跨市场支持 — ⛔ 无法验证

> ⛔ **本轮为样本数据，结论不完整。** 请用正式 licence 重跑：
> ```powershell
> .\scripts\Verify-Api.ps1 -Licence "你的licence"
> ```

## 统计

- 总请求数：**11**
- 完成时间：2026-09-02 15:56:58
